## What & why

<!-- What does this PR change, and what problem does it solve? -->

## How was it tested?

- [ ] `dotnet build -c Release` passes with 0 warnings
- [ ] `node --check` passes on any edited `WebUI/src/*.js`
- [ ] Launched the app — UI loads with no `WebError` lines in the launcher log
- [ ] (if it touches launching) launched a real modded instance end-to-end

## Performance claims (if applicable)

<!-- Ran the in-app Auto-Benchmark (instance → Performance) before/after? Paste both numbers. -->

## Checklist

- [ ] Changelog entry added to `launcher/CLAUDE.md` for user-visible changes
- [ ] No secrets/tokens in code, logs, or the web UI
